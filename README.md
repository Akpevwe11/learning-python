## Introduction to Decorators in Python

Decorators are functions that decorate other functions. We essentially wrap a function around another function.

**Why would one need a decorator**

That way we can give a function extra functionality without changing it.


let say you're working in a team, and want to avoid unnecessary code changes, a decorator will help you to 
avoid that.
